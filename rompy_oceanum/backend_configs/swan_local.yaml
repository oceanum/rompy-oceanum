# Backend configuration for SWAN model execution using native local backend
# This configuration is for native local execution on the host system (no containers)

type: local
timeout: 3600
command: "mpirun -n 2 swan.exe"
shell: true
capture_output: true
env_vars:
  OMP_NUM_THREADS: "2"
  ROMPY_MODEL: "swan"

